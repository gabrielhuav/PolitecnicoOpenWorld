package ovh.gabrielhuav.pow.features.interiores.escom.viewmodel

// ─────────────────────────────────────────────────────────────────────────────────────────────
// TOMBSTONE (2026-06-22): `MetrobusInteriorViewModel` se UNIFICÓ con `MetroInteriorViewModel` en
// `TransitInteriorViewModel` (mismo paquete), dirigido por `TransitSystemConfig` (catálogo
// `TransitSystems.METRO` / `.METROBUS`). Las pantallas crean el VM con
// `TransitInteriorViewModel.Factory(context, config, stationName, spawnX, spawnY)`.
// Toda la lógica vive AHORA una sola vez (la duplicación metro⇄metrobús del VM se eliminó).
// No queda nada aquí; archivo conservado solo como lápida (se puede borrar). Ver 05/06/09.
// ─────────────────────────────────────────────────────────────────────────────────────────────
