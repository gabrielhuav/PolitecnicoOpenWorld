<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 🆕 (2026-07-26) Necesario para el WifiLock del multijugador Wi-Fi (LAN) de "HUELUM VS.
         GOYA": mantiene el Wi-Fi despierto durante la sesión para que el power-save NO mate el
         socket TCP mientras se elige peleador (síntoma: "SELECT_CHARACTER → enlace muerto").
         WAKE_LOCK es permiso NORMAL (install-time, sin prompt de runtime, sin impacto en Data
         Safety de Play). Lo pide WifiManager.WifiLock.acquire(). -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 🆕 (2026-07-26) Para el MulticastLock del AUTODESCUBRIMIENTO LAN (SfLanDiscovery): sin él,
         algunos Wi-Fi filtran los paquetes UDP broadcast y el invitado no "vería" la sala del host.
         CHANGE_WIFI_MULTICAST_STATE es permiso NORMAL (sin prompt de runtime, sin impacto en Data
         Safety de Play). El tráfico es efímero dispositivo-a-dispositivo en la LAN. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- BLUETOOTH (multijugador local de "HUELUM VS. GOYA", sin internet).
         Legacy (≤ Android 11): permisos normales, sin runtime.
         Android 12+: CONNECT/ADVERTISE/SCAN en runtime; neverForLocation es CRÍTICO:
         sin ese flag el escaneo contaría como acceso a UBICACIÓN (el FINE_LOCATION de
         arriba se justifica SOLO por los mapas). Ver AUDIT_SF_MULTIPLAYER.md. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />

    <application
        android:name=".PowApplication"
        android:allowBackup="false"
        android:allowAudioPlaybackCapture="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PolitecnicoOpenWorld">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.PolitecnicoOpenWorld">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
    </application>

</manifest>
